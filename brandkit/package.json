{
  "name": "@respeak3/brandkit",
  "version": "1.0.0",
  "description": "HovaRehab / ReSpeak3 design token system — single source of truth for iOS and web.",
  "private": true,
  "scripts": {
    "gen:ios": "ts-node scripts/generate-ios.ts",
    "gen:web": "ts-node scripts/generate-web.ts",
    "gen":     "npm run gen:ios && npm run gen:web"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node":     "^10.9.2",
    "typescript":  "^5.4.0"
  }
}
