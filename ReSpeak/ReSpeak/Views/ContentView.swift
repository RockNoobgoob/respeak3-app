// ContentView.swift
// ReSpeak
// Created: 2026-03-01
//
// Root content view. Displays the ThemeDemoView during development so the
// full HovaRehab design system can be visually verified before building
// product screens on top of it.
//
// Replace the body with the real app navigation structure once the theme
// is approved by the design team.

import SwiftUI

struct ContentView: View {
    var body: some View {
        ThemeDemoView()
    }
}

#Preview {
    ContentView()
}
